export interface userLoginDTO {
  email: string;
  password: string;
}

export interface createUserDTO{
    name:string
    email:string
    password:string
}

export interface userPayloadDTO{
    name:string,
    email:String,
    id:number
}